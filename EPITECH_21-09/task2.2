ls -p | paste -sd

ls -p: lista os arquivos/pastas sem os ocultos (arquivos começando com . 
já ficam de fora por padrão), em ordem alfabética, e adiciona / no final dos diretórios

paste -sd: junta tudo numa linha só, separado por vírgulas